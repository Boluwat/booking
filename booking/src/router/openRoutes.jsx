import { SignUpContainer } from "../containers";
import contants from "../utils/contants";

const openRoutes = [
    {
        path: contants.Routes.SignUp,
        element: <SignUpContainer />
    }
];

export default openRoutes;
